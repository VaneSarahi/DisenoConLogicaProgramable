module RETO_w (
	input MAX10_CLK1_50,
	input [0:0] KEY, 
	input [0:0] SW, 
	output [6:0] ARDUINO_IO
	

);

RETO WRAP(
	.SW(SW[0]),
	.KEY(~KEY[0]),
	.clk(MAX10_CLK1_50),
	.pwm_x(ARDUINO_IO[2]),
	.pwm_y(ARDUINO_IO[3]),
	.pwm_z(ARDUINO_IO[4])

);



endmodule 